parameters:
  rhnuser: {{ rhnuser | quote }}
  rhnpassword: {{ rhnpassword | quote }}

compute:
  image: "rhel9"
  memory: 16384
  numcpus: 4
  disks:
    - size: {{ disk_size_root }}
      interface: "sata"
  nets: 
{% for net in libvirt_networks | map(attribute='name') | list %}
    - {{ net }}
{% endfor %}