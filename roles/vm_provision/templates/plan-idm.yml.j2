parameters:
  rhnuser: {{ rhnuser | quote }}
  rhnpassword: {{ rhnpassword | quote }}

idm:
  image: "rhel8"
  memory: 8192
  numcpus: 8
  disks:
   - size: {{ disk_size_root }}
     interface: "sata"
  nets: 
{% for net in libvirt_networks | map(attribute='name') | union(['default']) | list %}
    - {{ net }}
{% endfor %}